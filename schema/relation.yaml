# Relation Schema
# 연결의 구조. 누구와 어떻게 연결되는가.
# ---
# version: 0.0
# status: draft

relation:
  internal: []      # 내부 연결. 멤버 간 관계 구조.
  external: []      # 외부 연결. 다른 Kytos OS와의 관계.
  mycelium: []      # 균사체 망. 연결된 네트워크 목록.
  exchange: ~       # 교환 방식. 무엇을 주고받는가.
